'use strict';

var apiConfigs = require('./api-configs.js');

function getSchemaData(outputDir, apiType, apiVersion) {
    const config = apiConfigs.apiConfigs[apiType];
    return {
        schema: config.schema.replace("%%API_VERSION%%", apiVersion ? `/${apiVersion}` : ""),
        schemaFile: `${outputDir}/${config.schemaFile.replace("%%API_VERSION%%", apiVersion ? `-${apiVersion}` : "")}`,
    };
}

exports.getSchemaData = getSchemaData;
//# sourceMappingURL=get-schema-data.js.map
