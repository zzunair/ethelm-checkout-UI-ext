import '../../../runtime/crypto/types.mjs';
import { crypto as cryptoVar } from '../../../runtime/crypto/crypto.mjs';

function nonce() {
    const length = 15;
    // eslint-disable-next-line no-warning-comments
    // TODO Remove the randomBytes call when dropping Node 16 support
    const bytes = cryptoVar.getRandomValues
        ? cryptoVar.getRandomValues(new Uint8Array(length))
        : cryptoVar.randomBytes(length);
    const nonce = bytes
        .map((byte) => {
        return byte % 10;
    })
        .join('');
    return nonce;
}

export { nonce };
//# sourceMappingURL=nonce.mjs.map
