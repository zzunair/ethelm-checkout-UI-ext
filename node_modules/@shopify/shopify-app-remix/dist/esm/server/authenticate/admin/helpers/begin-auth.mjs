async function beginAuth(params, request, isOnline, shop) {
    const { api, config } = params;
    throw await api.auth.begin({
        shop,
        callbackPath: config.auth.callbackPath,
        isOnline,
        rawRequest: request,
    });
}

export { beginAuth };
//# sourceMappingURL=begin-auth.mjs.map
